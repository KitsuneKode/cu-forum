{
  "name": "@cu-forum/api",
  "module": "./src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun ./src/server.ts",
    "dev": "bun --watch run ./src/server.ts",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "bun run lint",
    "lint:fix": "bun run lint --fix"
  },
  "devDependencies": {
    "@cu-forum/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "prettier": "^3.6.2",
    "prettier-plugin-sort-imports": "^1.8.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@cu-forum/backend-common": "workspace:*",
    "@cu-forum/common": "workspace:*",
    "@cu-forum/auth": "workspace:*",
    "@cu-forum/store": "workspace:*",
    "@cu-forum/trpc": "workspace:*",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
