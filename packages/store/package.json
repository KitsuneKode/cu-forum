{
  "name": "@cu-forum/store",
  "module": "src/index.ts",
  "private": true,
  "scripts": {
    "db:generate": "bunx prisma generate",
    "db:migrate": "bunx prisma migrate ",
    "db:migrate:dev": "bunx prisma migrate dev",
    "db:migrate:deploy": "bunx prisma migrate deploy",
    "db:seed": "bun run src/scripts/seed.ts",
    "db:studio": "bunx prisma studio",
    "db:reset": "bunx prisma migrate reset -f"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@cu-forum/typescript-config": "workspace:*",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "prisma": "^6.11.1"
  }
}
